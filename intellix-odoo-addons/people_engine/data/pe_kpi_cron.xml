<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="cron_pe_kpi_hebdomadaire" model="ir.cron">
        <field name="name">PE — KPI hebdomadaire commercial/marketing (vendredi 17h)</field>
        <field name="model_id" ref="model_pe_kpi_snapshot"/>
        <field name="state">code</field>
        <field name="code">model.sudo().cron_kpi_hebdomadaire()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="nextcall" eval="(DateTime.now() + timedelta(days=1)).replace(hour=17, minute=0, second=0).strftime('%Y-%m-%d %H:%M:%S')"/>
        <field name="active" eval="True"/>
    </record>
</odoo>
